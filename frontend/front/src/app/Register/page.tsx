import React, { ReactElement } from "react"

export const Register :React.FC = ():ReactElement => {

    return(
        <>
            <h1>Aqui componente Register</h1>
        </>
    )
}

export default Register;