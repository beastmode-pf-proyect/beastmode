import React, { ReactElement } from "react"

export const Login :React.FC = ():ReactElement => {

    return(
        <>
            <h1>Aqui componente Login</h1>
        </>
    )
}

export default Login;